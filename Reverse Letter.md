Given a string, reverse it and omit all non-alphabetic characters.

Example
For string = "krishan", the output should be "nahsirk".

For string = "ultr53o?n", the output should be "nortlu".

Input/Output
[input] string string
A string consists of lowercase latin letters, digits and symbols.

[output] a string
